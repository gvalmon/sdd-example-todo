# Phase N — Title

**Status**: Not started | In progress | Complete
**Specs**: SPEC-NNN-SLUG (Title), SPEC-NNN-SLUG (Title)

## Goal

One paragraph — what capability this phase delivers end-to-end. Written so a reviewer can tell, without reading the rest of the file, whether this phase is worth starting.

### Deliverables

Bullet list of implementation artifacts (files, modules, UI regions, data shapes). Each bullet names *what gets built*, with `(see SPEC-NNN-SLUG)` references for the product design instead of restating it.

- `path/to/file.ext` — short purpose (see SPEC-NNN-SLUG § Section).

### Integration Points

Concrete wiring: module imports, DOM ids, event-handler names, storage keys, shape changes. Anything downstream phases will need to know.

## Implementation Notes

Code-level decisions and constants. Rendering / update approach. Temporary-scaffolding rationale. Known gotchas. Patterns to follow. This is the section where the stack-specific opinion lives.

## E2E Tests

Automated test scenarios, named and scoped. Link each to the spec scenario it verifies.

## Manual Testing (Success Path)

Step-by-step manual verification a human can follow in the running app to confirm this phase's capability works end-to-end. Every phase MUST produce a runnable build that makes these steps possible.

1. Start the app (`...`).
2. Do the thing.
3. Observe the outcome.
4. Do the next thing.
5. Observe the outcome.

## Out of Scope for This Phase

What this phase explicitly is NOT doing, even though the spec calls for it. These items belong to a later phase — naming them here prevents scope creep and prevents the reviewer from expecting them.
